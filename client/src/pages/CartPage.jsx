import {React} from 'react'
import CartCard from '../component/CartCard'

const CartPage = () => {
  return <div style={{
    overflow: 'auto'
  }}>
    <CartCard/>
  </div>
}

export default CartPage